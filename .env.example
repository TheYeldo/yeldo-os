# Optional. Used only by the server-side GitHub route handler.
# Create a fine-grained token with public repository read access if unauthenticated
# GitHub rate limits are too restrictive. Never prefix this with NEXT_PUBLIC_.
GITHUB_TOKEN=

# Public canonical URL used for metadata and sitemap generation.
NEXT_PUBLIC_SITE_URL=https://yeldo-os.vercel.app
